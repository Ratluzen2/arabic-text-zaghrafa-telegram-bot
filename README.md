# 🤖 Arabic Text Zaghrafa Telegram Bot

بوت تيليجرام بسيط مكتوب بلغة بايثون (Python) يقوم بزخرفة (تزيين) الكلمات العربية باستخدام رموز Unicode.

## ✨ الميزات

*   **زخرفة فورية:** يقوم البوت بزخرفة أي نص عربي ترسله إليه.
*   **سهولة الاستخدام:** يعمل كبوت عادي، فقط أرسل النص.
*   **بنية واضحة:** الكود مقسم إلى ملف منطقي لدالة الزخرفة وملف رئيسي لتشغيل البوت.

## 🚀 كيفية التشغيل

### المتطلبات

*   Python 3.8+
*   توكن (Token) بوت تيليجرام (يمكنك الحصول عليه من [BotFather](https://t.me/BotFather)).

### الخطوات

1.  **استنساخ المستودع:**

    ```bash
    git clone https://github.com/Ratluzen2/arabic-text-zaghrafa-telegram-bot.git
    cd arabic-text-zaghrafa-telegram-bot
    ```

2.  **إنشاء بيئة افتراضية وتثبيت التبعيات:**

    ```bash
    python3 -m venv venv
    source venv/bin/activate
    pip install -r requirements.txt
    ```

3.  **تعيين توكن البوت:**

    يجب تعيين توكن البوت كمتغير بيئة.

    ```bash
    export TELEGRAM_BOT_TOKEN="YOUR_BOT_TOKEN_HERE"
    ```

    > **ملاحظة:** استبدل `"YOUR_BOT_TOKEN_HERE"` بالتوكن الخاص بك.

4.  **تشغيل البوت:**

    ```bash
    python bot.py
    ```

    سيتم تشغيل البوت، ويمكنك الآن التحدث إليه على تيليجرام.

## ⚙️ هيكل المشروع

| الملف | الوصف |
| :--- | :--- |
| `bot.py` | الكود الرئيسي للبوت، يحتوي على دالة الزخرفة ويتعامل مع أوامر تيليجرام والرسائل. |

| `requirements.txt` | قائمة بمكتبات بايثون المطلوبة (`python-telegram-bot`). |
| `venv/` | البيئة الافتراضية للمشروع (يتم تجاهلها بواسطة `.gitignore`). |

## 📝 خريطة الزخرفة

خريطة الزخرفة الحالية موجودة في ملف `bot.py` ويمكنك تعديلها وتوسيعها لإضافة المزيد من أنماط الزخرفة.

```python
# مثال من الخريطة
ZAGHRAFA_MAP = {
    'ا': 'آ', 'أ': 'آ', 'إ': 'آ',
    'ب': 'ٻ',
    # ... المزيد
    ' ': ' 𓏲 ' # زخرفة للمسافة
}
```

---
تم التطوير بواسطة **Manus AI**.
